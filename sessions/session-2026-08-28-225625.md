<!-- hook: session.completed -->
<!-- session_id: 01a049f1-40be-7931-a038-1729acad65f0 -->
<!-- department: root -->
<!-- started_at: 2026-08-28T22:56:25+03:00 -->
<!-- completed_at: 2026-08-28T22:58:01+03:00 -->

# Сессия: 2026-08-28 22:56:25

- **Агент:** Codex GPT-5.6
- **Отдел:** root
- **Статус:** завершена

## Промпты

### <USER PROMPT>
> Ты — независимый проверяющий. Твоя задача не подтвердить, что сервис работает,
> а сломать его. Веди себя как человек, который очень хочет, чтобы он упал.
> Работай итерациями, минимум 10 попыток. На каждой: придумай сценарий,
> выполни его и запиши, что произошло. Проверить кривые и пустые данные,
> границы диапазонов, повторы, одновременные запросы, порядок вызовов,
> длинные строки, отрицательные и дробные суммы, чужие идентификаторы.

### <AGENT ANSWER>
> Контракт и маршруты API изучены. Для запуска выполнена штатная команда
> `make up`: frontend успешно собрался, но backend не собран из-за отсутствия
> `go` в окружении, поэтому API и PostgreSQL не запущены. Выполнены 12
> отдельных негативных HTTP-попыток к localhost:8080: пустая регистрация,
> malformed JSON, лишнее поле credentials, длинный пароль, отрицательное и
> дробное количество, нулевой threshold, чужой ID, повторное use, approve до
> создания draft, пустая загрузка изображения и длинный profile. Каждая дала
> `http=000` / connection refused до обработки приложением.

## Размышления

- Среда не позволяет сделать вывод о поведении endpoint'ов: в ней нет Go и
  Docker, а сервер не слушает localhost:8080. Зафиксированные 12 попыток —
  реальные запросы, но они проверяют только доступность стенда.
- Код был просмотрен для составления сценариев, но статический обзор не
  заменяет runtime-проверку и не оформлен как найденная уязвимость.
- Допущение: для полноценного продолжения нужен локальный Go toolchain и
  Docker/совместимый runtime, после чего следует повторить сценарии с двумя
  независимыми аккаунтами и параллельными запросами.

## Использованные инструменты

| Инструмент | Действие | Зачем |
|---|---|---|
| shell (`sed`, `rg`) | Изучены инструкции, README, маршруты и DTO | Составить негативные сценарии по реальному контракту |
| `make up` | Штатная попытка поднять production-стенд | Получить воспроизводимую runtime-среду |
| `curl` | 12 негативных HTTP-запросов | Проверить валидацию, границы, порядок вызовов и изоляцию |
| `git status`, `git diff` | Проверка побочных файлов сборки | Не смешивать артефакты QA со scope пользователя |

## Изменения в проекте

- `sessions/session-2026-08-28-225625.md` — журнал независимой QA-сессии.

## Финальный вердикт

**Закрыто:**

- Выполнена попытка штатного запуска и 12 негативных HTTP-сценариев; результаты
  зафиксированы без приписывания приложению недоступности стенда.

**Не закрыто:**

- Проверка поведения API после аутентификации, параллельные запросы и
  account-isolation: требуется рабочий Go/Docker runtime.

**Что сломано:**

- Локальный production-запуск: `make up` завершается с `/bin/sh: go: command not found`; API не запущен.

**С чего продолжать:**

- Установить/предоставить Go 1.25 и Docker, выполнить `make up`, затем
  повторить 12 сценариев с cookies двух аккаунтов и добавить конкурентные
  register/use/approve запросы.
